<div class="indexCate">
<h1>Edit category</h1>
<%= form_with model: @rec_category, url: admin_rec_category_path(@rec_category), local: true do |form| %>

  <% if @rec_category.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@rec_category.errors.count, "error") %> prohibited
        this rec_category from being saved:
      </h2>
      <ul>
        <% @rec_category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :id %><br>
    <%= form.text_field :id %>
  </p>

  <p>
    <%= form.label :name %><br>
    <%= form.text_area :name %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>

<%= link_to 'Back', admin_rec_category_path, :class => 'newCate' %>
</div>
