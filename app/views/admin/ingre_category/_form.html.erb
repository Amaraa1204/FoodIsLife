
<%= stylesheet_link_tag 'admin', media: 'all', 'data-turbolinks-track': 'reload' %>

<%= form_with model: @ingrecategory, url: @newpath, local: true do |form| %>
 
  <% if @ingrecategory.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@ingrecategory.errors.count, "error") %> prohibited
        this ingrecategory from being saved:
      </h2>
      <ul>
        <% @ingrecategory.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
  <p id="labels">
  <%= form.label :image %><br>
  <span class="sbtn"><%= form.file_field :image, class: "image" %></span>
</p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
