<h1>Index page</h1>
<%= link_to 'New User', new_user_path %>
<br>
<table>
	<tr>
		<th>Username</th>
		<th>Email</th>
	</tr>

	<% @user.each do |user| %>
	<tr>
		<td><%= user.user_name %></td>
		<td><%= user.email %></td>
		<td><%= link_to 'Show', user_path(user) %></td>
	</tr>
	<% end %>
</table>