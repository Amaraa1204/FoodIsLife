
<h1>New User</h1>

<%= form_with scope: :user, url: user_index_path(@user), local: true do |form| %>
  <% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from bein saved: </h2>
    <p>
      <%= form.label :email %><br>
      <%= form.text_field :email %>
    </p>
    <p>
      <%= form.label :username %><br>
      <%= form.text_field :user_name %>
    </p>
    <p>
    <%= form.label :password %><br>
    <%= form.text_field :password %>
    </p>
   
    <p>
      <%= form.submit %>
    </p>
  <% end %>

  <%= link_to 'Back', user_index_path %>